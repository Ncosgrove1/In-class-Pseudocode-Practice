--- Task #1 --- 

DECLARE userInput : STRING
DECLARE result : STRING

DISPLAY "Please enter a phrase: "
INPUT userInput

// Error Checking: Check if input is empty //
IF LENGTH(userInput) == 0 THEN
    DISPLAY "Error: No input provided."
ELSE
    // Convert to uppercase using a built-in function // 
    result ← UCASE(userInput)
    DISPLAY "Output: ", result
ENDIF
--- (under the assumption that .upper() or toUpperCase() is used) ---

--- Task #2 --- 

FUNCTION get_number(prompt)
    WHILE true:
        PRINT prompt
        READ input
        IF input is numeric THEN RETURN input
        ELSE PRINT "Invalid input."

START
    num1 = get_number("Number 1: ")
    num2 = get_number("Number 2: ")
    PRINT "Output: " + (num1 + num2)
END

--- Task #3 --- 

 SET totalSum = 0
    SET continueInput = "y"

    WHILE continueInput is "y" or "Y" DO
        PRINT "Number: "
        GET userInput

    // Error Checking: Ensure the input is a valid number // 
    IF userInput is numeric THEN
            totalSum = totalSum + userInput
    ELSE
        PRINT "Invalid entry. Please enter a numerical value."
        CONTINUE // Skip to the next iteration
    END IF

    PRINT "Another number? (y/n)"
    GET continueInput
        
    // Error Checking: Handle cases where user types something other than y/n //
    WHILE continueInput is NOT "y", "Y", "n", or "N" DO
            PRINT "Please enter 'y' for yes or 'n' for no."
            GET continueInput
        END WHILE
    END WHILE

    PRINT "Output: " + totalSum

--- Task #4 --- 

BEGIN
    SET playAgain = "Y"

    WHILE playAgain == "Y" OR playAgain == "y" DO
        PRINT "Max Number: "
        READ userInput
        
        // Error Check: Ensure input is a valid positive integer //
        IF userInput IS NOT numeric OR userInput < 0 THEN
            PRINT "Error: Please enter a valid positive whole number."
            CONTINUE // Skip to the next iteration of the loop
        END IF

        SET maxNum = CONVERT_TO_INT(userInput)
        
        // Generate random number (inclusive of 0 and maxNum)
        SET result = RANDOM_INT(0, maxNum)
        
        PRINT "Output: " + result

        PRINT "New Number (y/n)? "
        READ playAgain

  // Error Check: Basic validation for y/n response // 
        WHILE playAgain != "Y" AND playAgain != "y" AND playAgain != "N" AND playAgain != "n" DO
            PRINT "Invalid input. Please enter Y or N: "
            READ playAgain
        END WHILE
    END WHILE

    PRINT "Bye!"
END

--- Task #5 --- 

